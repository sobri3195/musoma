import {json,midtrans,mapStatus,supabase} from './_lib.js';
export default async function handler(req,res){if(req.method!=='GET')return json(res,405,{error:'Metode tidak diizinkan'});const id=String(req.query?.order_id||'');if(!/^SOBRI-[\w-]+$/.test(id))return json(res,400,{error:'Nomor pesanan tidak valid'});try{const r=await midtrans('/v2/'+encodeURIComponent(id)+'/status');if(!r.ok)return json(res,404,{error:'Status tidak ditemukan'});const data=await r.json(),status=mapStatus(data.transaction_status);await supabase('orders?order_number=eq.'+encodeURIComponent(id),{method:'PATCH',body:JSON.stringify({status})});return json(res,200,{order_id:id,status})}catch{return json(res,500,{error:'Status belum dapat diperiksa'})}}
